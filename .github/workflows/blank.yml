name: CICD

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:      
      - name: Make build start
        run: echo hello build step

  deploy:
    needs: build
    runs-on: [ubuntu-latest]
    steps:
      - name: Make deploy start
        run: echo hello build step
